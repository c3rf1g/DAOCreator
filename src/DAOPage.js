import React from 'react';

const DaoPage = (props) => {
    console.log(props.match.params.id)
    // const isDaoContract = (address) => {
    //     const contract = new web3.eth.Contract(abi)
    // }
    return (
        <div>

        </div>
    );
};

export default DaoPage;